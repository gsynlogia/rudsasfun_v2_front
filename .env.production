# Production Environment Variables
# This file is used when NODE_ENV=production (npm run build + npm start)
# Can be committed to git (contains production configuration)

# Backend API URL for production
NEXT_PUBLIC_API_URL=https://api.rezerwacja.radsas-fun.pl

# Fake Data - always false in production
NEXT_PUBLIC_FAKE_DATA=false

# Off Portal - set to true to show maintenance page
NEXT_PUBLIC_OFF_PORTAL=true
